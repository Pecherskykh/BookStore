import { PrintingEditionsFilterModel } from './printing-editions-filter-model';

describe('PrintingEditionsFilterModel', () => {
  it('should create an instance', () => {
    expect(new PrintingEditionsFilterModel()).toBeTruthy();
  });
});
