import { PrintingEditionModel } from './printing-edition-model';

describe('PrintingEditionModel', () => {
  it('should create an instance', () => {
    expect(new PrintingEditionModel()).toBeTruthy();
  });
});
