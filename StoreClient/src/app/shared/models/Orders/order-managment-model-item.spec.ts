import { OrderManagmentModelItem } from './order-managment-model-item';

describe('OrderManagmentModelItem', () => {
  it('should create an instance', () => {
    expect(new OrderManagmentModelItem()).toBeTruthy();
  });
});
