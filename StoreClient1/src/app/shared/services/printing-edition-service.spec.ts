import { PrintingEditionService } from './printing-edition-service';

describe('PrintingEditionService', () => {
  it('should create an instance', () => {
    expect(new PrintingEditionService()).toBeTruthy();
  });
});
